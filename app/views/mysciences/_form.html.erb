<%= form_with(model: myscience) do |form| %>
  <% if myscience.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(myscience.errors.count, "error") %> prohibited this myscience from being saved:</h2>

      <ul>
        <% myscience.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
